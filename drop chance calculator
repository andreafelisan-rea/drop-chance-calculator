#include <iostream>
#include "function1.h"
#include "function2.h"
using namespace std;

int main()
    double dropRate ;
    int attempts;
    
    cout<<"Enter the drop rate (as a decimal): ";
    cin>>dropRate;
    cout<<"Enter the number of attempts: ";
    cin>>attempts;
    
    double chance = calculatorDropChance(dropRate,attempts);
    cout<<"The chance of getting the drop in "<<attempts;
    
    
}
